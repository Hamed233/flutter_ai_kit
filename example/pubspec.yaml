name: flutter_ai_kit_example
description: Example app for flutter_ai_kit
publish_to: 'none'

environment:
  sdk: ^3.5.3
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_ai_kit:
    path: ../
  shared_preferences: ^2.2.2
  google_fonts: ^6.1.0
  flutter_markdown: ^0.6.18

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
